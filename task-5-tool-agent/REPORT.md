# Task 5 修复与验证记录

## 当前结论

旧版报告中的 10/10 结果依赖固定题目参数改写和答案后附加工具证据，不能代表一般工具调用能力，现不再把该结果作为修复版成绩。修复后已在 AutoDL 的真实 Qwen 服务上重新生成十题 Trace：7/10 成功；失败的第 4、5、9 题均依赖 Wikipedia，而运行环境访问 Wikipedia API 超时。该结果如实保留网络失败，不再使用静态答案兜底。

## 修复项

- 删除针对 `2026`、100 以内质数、固定文件目录和特定 Wiki 主题的恢复及参数规范化逻辑。
- 工具名称和 `Action Input` 完全来自当前一轮模型输出，Agent 不再静默修改参数或替模型生成后续调用。
- 模型格式错误或过早作答时，只反馈协议 Observation，由模型重新阅读原始问题并自行产生调用。
- 删除最终答案自动附加 Observation 的行为；完整工具证据仍保留在 Trace 的 `steps` 中。
- Wikipedia 改为真实 API 查询，不再以已知评测主题的静态摘要冒充查询结果。
- Trace 生成器取消固定题号，支持通过命令行指定任务文件、题号和输出位置。

## 回归与端到端结果

2026-09-06 在 Python 3.12 环境运行，8/8 项通过。

运行命令：

```bash
python -m unittest discover -s eval -p "test_*.py" -v
```

覆盖项目：

- `sqrt(81)` 保持为 `sqrt(81)`，计算结果为 9。
- 10 以内质数程序保持模型原始代码，输出 17。
- 文件检索保留模型指定的 `eval/fixtures/custom_location` 路径。
- `Ada Lovelace analytical engine` 查询不被替换为旧评测主题。
- 无效模型格式不会触发隐藏工具调用。
- 没有工具证据的 Final Answer 会被拒绝，后续工具仍由模型选择。

2026-09-06 在 AutoDL 启动 Qwen 后重新运行完整评测，结果为 7/10。第 1、2、3、6、7、8、10 题成功；第 4、5、9 题的 Trace 均记录 Wikipedia 请求失败。单工具自检中的计算器、Python、文件检索均通过；Wikipedia 项因外部网络超时被明确标记为跳过。新的 `eval/traces.json` 和 `eval/result.json` 保存了本次真实结果。

## 结果边界

回归测试验证 Agent 编排和参数传递，十题评测验证当前 Qwen 服务下的端到端行为。7/10 不能解释为模型在所有工具均可用时的上限，因为三项失败受 Wikipedia 连通性影响；同样也不能把这三项擅自记为成功。网络恢复后可原样重跑。评测按最终答案本身判分，不搜索整个 Trace 中的工具输出关键词。
